# DCO091 — FM1-QM.SLQ001 Document Change Order form (Word)

Working file: redlined Word in `QMSInProcess\DCO091`.

This form carries most of the NC #15 user-visible change (per-row calendar dates and originator attestation).

## Source of truth for layout and paste text

`docs/DCO091/MASTER_EFFECTIVE_DATE_NC15_EDITING_GUIDE.md`

Apply in full:

- Section E.3 (table FIND and REPLACE for header strip and document table column).
- Section F (numbered Word layout steps 1 through 10).

## Coordination with QM.SLQ001

The new column title must match QM.SLQ001 Completing DCO form wording exactly:

`Proposed effective date (calendar, GMP use)`

The originator attestation block text is in MASTER section E.1.5 (INSERT block). Paste that block onto the form below the document table or in the company-standard attestation area.

## Coordination with Silq eQMS

After form release, blank FM1 must be filed in Admin Docs per QM.SLQ001. Instructions on the form footer or instruction page must tell originators to obtain the blank from Silq eQMS Admin Docs, library Forms, Templates and Travelers, or from the document administrator (same wording as QM.SLQ001 PASTE Completing DCO opening).

## Verification (form only)

- Header strip: no `Req. Effective Date:` paired with `R +` as primary control.
- Document table: new column present with exact title above; one date cell per document row.
- Attestation block with five checkboxes present.
- Instructions: no R+0 or R+7 as primary rule; text says one calendar date per row and approver confirmation.
- No FileHold workflow or password-signature language in instructions or hidden text (turn on Show hidden text in Word).
