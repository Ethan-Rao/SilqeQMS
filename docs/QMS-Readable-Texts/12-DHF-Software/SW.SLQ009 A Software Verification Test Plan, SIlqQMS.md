

SilqQMS Document Management Software System


Software Verification Test Plan

Purpose
The purpose of this document is to define the testing activities required to verify the SilqQMS electronic document management system. The implementation of this plan is intended to ensure the functionality of SilqQMS meets the defined inputs/requirements. This software verification plan summarizes the proposed testing activities that are required to provide objective evidence SilqQMS functions and performs as intended. Verification consists of both manual test procedures and automated software tests (pytest).
Scope
The following software verification test plan applies only to testing of the SilqQMS electronic document management system. The scope of testing covers the two SilqQMS modules and supporting infrastructure defined in SW.SLQ007 (Software Validation Plan, SilqQMS) and SW.SLQ008 (Product Requirements Specification, SilqQMS):
Document Control module, Admin Docs Libraries module, Supporting infrastructure (authentication, role-based access control, audit trail, file storage, CSRF protection, and security headers)
Documents
Reference Documents
SW.SLQ007	Software Validation Plan, SilqQMS
SW.SLQ008	Product Requirements Specification, SilqQMS
Associated Forms
N/A
Definitions
Abbreviations / Acronyms
N/A
General Definitions
SilqQMS:  A custom-developed, web-based electronic document management system (EDMS) used by SILQ to store, organize, retrieve, and lifecycle-manage controlled QMS documents and records.
Design Input: The physical and performance requirements relating to the device and its intended use. These requirements establish a basis for performing subsequent design tasks, and focuses on the needs of the user and patients.
Design Output: The results of a design effort at each design phase and at the end of the total design effort.  The finished design output is the basis for the device master record.  The total finished design output consists of the device, its packaging and labeling, and the device master record.
Verification:  Confirmation, by examination and provision of objective evidence, that specified requirements (design inputs) have been fulfilled by the product design (design outputs).
Responsibilities
It is the responsibility of the Quality Assurance personnel to implement this design verification test plan and to ensure the test plan status is updated as testing is completed.
Verification Test Plan
The design verification test plan has been developed to define the required testing activities for the SilqQMS electronic document management system. Testing has been divided into the test cases described in Table 1. Each test case incorporates either a single set or multiple sets of test activities. It is intended that the sum result of the activities defined in each test case will provide sufficient evidence that the software system meets all requirements for functionality.
Verification testing is to be documented using a test procedure (SW.SLQ010). The test procedure must contain identification of the tester, test date, and software version being tested. It must also include a list of required equipment and required test set-up instructions.
In addition to manual test procedures, SilqQMS has automated software tests (pytest) that provide supplementary verification evidence. Automated test results will be attached to the final validation report (SW.SLQ011). Automated tests do not replace manual OQ testing — both are needed for a complete validation.
Sample size / rationale
Because this is a software verification test where there is no variability, the sample size is n = 1.
Completed test procedure (SW.SLQ010) will be attached to the final validation report (SW.SLQ011). Automated test results (pytest output) will also be attached to the final validation report as supplementary verification evidence.
With the completion of testing activities, the testing status in Table 1 is to be updated as necessary to reflect the current status.
Table 1:	Software Verification Test Plan:  SilqQMS

[Table]
| Test Case (#) | Test Purpose / General Description | Corresponding Requirements | Analysis Method (Sample Size) | Test Protocol No. / ID | Status (Pass/Fail) | Additional Info |
| --- | --- | --- | --- | --- | --- | --- |
| DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES | DESIGN VERIFICATION ACTIVITIES |
| Document Creation and Lifecycle | Tests creating a new document with required fields (document number, title, type), uploading a file to the draft revision, releasing the revision with a required reason, creating a new revision from a Released document, and verifying lifecycle state transition enforcement. Verifies that document numbers are unique and revision letters follow sequential alphabetical order. | SRS-1.1, SRS-1.2, SRS-1.3, SRS-1.4, SRS-1.8, SRS-1.10, SRS-1.11 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Automated test: test_document_control.py:: test_document_control_vertical_slice_creates_releases_downloads_and_audits` covers create  → upload → release → download flow (SRS-1.1, SRS-1.2, SRS-1.3, SRS-1.10). Manual testing should also verify negative cases: cannot release without file, cannot create revision from Draft, cannot upload to a revision that already has a file. |
| Document Obsoleting | Tests obsoleting a Released document by providing a required reason. Verifies the status changes to "Obsolete" and an audit event is recorded with the reason. Tests that the obsolete action requires a reason (rejection if blank). | SRS-1.5 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: |  |
| Document Download and View | Tests downloading files from documents in Draft, Released, and Obsolete status. Tests inline viewing for browser-renderable types (PDF, images) and server-side rendering for office formats (.docx, .xlsx, .xls, .csv). Verifies that downloads are served as attachments with the original filename. | SRS-1.6, SRS-1.7 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Test download from all three document statuses. Test inline view for at least one PDF and one office format file. |
| File Integrity | Tests that SHA-256 hash and file size are computed and stored upon file upload to a Document Control revision. Verifies the stored hash, file size, filename, and content type in the file record. | SRS-1.9 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Can be verified by examining the database record after upload, or by downloading the file and comparing its independently computed SHA-256 hash to the stored value. |
| Obsolete Document Safeguards | Tests that obsolete documents display a red "OBSOLETE" badge in the document list. Tests that the detail page shows a warning banner with the obsolete reason. Verifies that downloading from an obsolete document records a `doc.download_obsolete` audit event and viewing records a `doc.view_obsolete` event. Verifies that the "Create next revision" and "Obsolete document" action forms are hidden. | SRS-2.1, SRS-2.2, SRS-2.3, SRS-2.4, SRS-2.5 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Automated tests cover the distinct audit events: `test_edms_improvements.py::test_obsolete_document_download_creates_distinct_audit_event` (SRS-2.3) and `test_edms_improvements.py::test_obsolete_document_view_creates_distinct_audit_event` (SRS-2.4). Manual testing should verify the visual elements: badge in list view (SRS-2.1), warning banner on detail page (SRS-2.2), and hidden action forms (SRS-2.5). |
| Admin Docs Library Operations | Tests browsing all 11 named libraries. Tests creating a folder within a library, uploading a single file, uploading multiple files, downloading a file, viewing a file inline (including server-side rendering), moving a file between libraries/folders, and navigating folder hierarchy with breadcrumb links. | SRS-3.1, SRS-3.2, SRS-3.3, SRS-3.4, SRS-3.5, SRS-3.6, SRS-3.7, SRS-3.8 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Verify all 11 library names match the specification. Test breadcrumb navigation by creating a nested folder structure and confirming each breadcrumb element links correctly. |
| Authentication and Session Security | Tests login with valid credentials, login with invalid credentials, logout, session timeout after 60 minutes of inactivity (sliding window), and login rate limiting (blocked after 5 failed attempts within 5 minutes). Verifies that passwords are stored as hashed values. Verifies that login, logout, and failed login events are recorded in the audit trail. | SRS-4.1, SRS-4.2, SRS-4.3, SRS-4.4, SRS-4.5 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Automated test `test_edms_improvements.py::test_session_timeout_is_60_minutes` verifies the session timeout configuration (SRS-4.3). Password hashing (SRS-4.2) can be confirmed by database inspection (password_hash column contains a bcrypt hash, not plaintext). Manual testing: verify the rate limiting message appears after 5 failed attempts. |
| Access Control | Tests that unauthenticated users are redirected to the login page when accessing a protected route. Tests that authenticated users without the required permission receive a 403 Forbidden response. Verifies that permission denials are logged. | SRS-5.1, SRS-5.2, SRS-5.3 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Test: access a protected page (e.g., /admin/modules/document-control/) without logging in — expect redirect to /auth/login. Test: log in as a user without the required permission — expect 403 response. Check application logs for the RBAC denial entry. |
| Audit Trail | Tests that all significant actions are recorded in the audit trail with the required fields (timestamp, actor email, action type, entity type/ID, reason, metadata, client IP, request ID). Tests filtering by action, actor email, and date range. Tests CSV export with all columns. Tests that the export action itself is recorded in the audit trail. Verifies that no mechanism exists to modify or delete audit records. | SRS-6.1, SRS-6.2, SRS-6.3, SRS-6.4, SRS-6.5, SRS-6.6 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | Automated tests cover CSV export (`test_edms_improvements.py::test_audit_trail_csv_export`, SRS-6.4/6.5 partial) and date filtering (`test_edms_improvements.py::test_audit_trail_csv_export_with_date_filter`, SRS-6.3 partial). Manual testing: verify the audit trail UI shows expected actions after performing operations. Verify the CSV contains all expected columns. Verify no delete/edit buttons exist in the audit trail UI (SRS-6.6). |
| Security Controls | Tests CSRF protection by attempting a state-changing POST request without a CSRF token and verifying rejection. Verifies that the login endpoint is exempt from CSRF. Tests security headers by inspecting HTTP response headers for Content-Security-Policy, X-Content-Type-Options (nosniff), X-Frame-Options (DENY), and Referrer-Policy (strict-origin-when-cross-origin). | SRS-7.1, SRS-7.2 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | CSRF: submit a POST form without a CSRF token and verify a 400 response with "CSRF token missing or invalid." Security headers: use browser developer tools (Network tab) to inspect response headers on any page load. |
| Data Integrity — No-Delete Verification | Confirms that no delete functionality exists for documents, document revisions, or document files in the Document Control module. Confirms that no delete functionality exists for uploaded files in the Admin Docs Libraries module. Confirms that obsolete documents remain visible and accessible in the document list (not hidden or removed). | SRS-8.1, SRS-8.2, SRS-8.3 | n = 1 | SW.SLQ010 | PASS  FAIL  Other: | This is a verification inspection rather than a functional test. Inspect the Document Control and Admin Docs UIs for the absence of any delete buttons, links, or menu options. Navigate to the document list and confirm obsolete documents appear with the OBSOLETE badge. |
