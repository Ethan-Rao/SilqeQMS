

[Table]
| Tester name: |  | Test summary | Test summary |
| --- | --- | --- | --- |
| Test date: |  | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Silq eQMS version tested: |  | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Reference documentation | SW.SLQ008 Product Requirements Specification, Silq eQMS SW.SLQ009 Software Verification Test Plan, Silq eQMS | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Equipment needed | 1 laptop/desktop computer with internet capability and a modern web browser (Chrome, Firefox, Edge, or Safari) | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Sampling plan / rationale: | N = 1; Because there is no variability in software, software verification testing requires sample size of 1. | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Reviewed by (sign/date): |  | Test Case 1   Test Case 2 Test Case 3 Test Case 4 Test Case 5  Test Case 6    Test Case 7    Test Case 8    Test Case 9    Test Case 10  Test Case 11  Overall Test Result | PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL      PASS    FAIL   PASS    FAIL |
| Test Setup | Prior to testing, the system administrator (or equivalent authorized user) shall perform the following:  1. Confirm Silq eQMS is deployed to the production environment and that the URL `https://Silq eQMS.com` (DigitalOcean App Platform deployment URL) is reachable over HTTPS in the tester's browser. 2. Record the Silq eQMS version being tested (commit SHA or release tag) on the cover page. 3. Create the following test user accounts via Admin > Accounts:    - `qa_tester@silq.test` -- assign role `admin` (all Document Control and Admin Docs permissions). This is the primary test account. - `readonly_tester@silq.test` -- assign a role with only `admin.view` and `docs.view` permissions, and NO `docs.create`, `docs.edit`, `docs.release`, or `docs.obsolete` permissions. This account is used to verify Access Control (Test Case 8).    - Record both account passwords securely for the duration of testing. 4. Confirm there are no existing documents in Document Control with the test document numbers used below (`SRS-TEST-001`, `SRS-TEST-002`). If they exist from a prior test run, use distinct numbers (e.g., `SRS-TEST-001b`) and record the substitution in the Observed Results column. 5. On the tester's laptop, prepare the following test files (any suitable content):    - `test_doc.pdf` -- a small PDF file (< 1 MB).    - `test_doc.docx` -- a small Microsoft Word file.    - `test_doc.xlsx` -- a small Microsoft Excel file.    - `test_image.png` -- a small PNG image.    - `test_data.csv` -- a small CSV file.    - `large_file.bin` -- a binary file greater than 50 MB but less than 100 MB (used for size-limit verification). 6. Have available a command-line terminal with either `curl` or a browser with developer tools (Network tab) to inspect HTTP headers and to post CSRF-protected requests. 7. Have available a utility capable of computing a SHA-256 hash of a file (e.g., `certutil -hashfile <file> SHA256` on Windows, `shasum -a 256 <file>` on macOS/Linux, or PowerShell `Get-FileHash -Algorithm SHA256`). No special server-side configuration is required beyond the standard production deployment. Silq eQMS does not have document schemas, cabinets, drawers, metadata fields, or workflow approval groups analogous to FileHold. The Silq eQMS equivalents are the 11 named Admin Docs Libraries (fixed; defined in SRS-3.1) and the Document Control module's Draft/Released/Obsolete lifecycle. | Prior to testing, the system administrator (or equivalent authorized user) shall perform the following:  1. Confirm Silq eQMS is deployed to the production environment and that the URL `https://Silq eQMS.com` (DigitalOcean App Platform deployment URL) is reachable over HTTPS in the tester's browser. 2. Record the Silq eQMS version being tested (commit SHA or release tag) on the cover page. 3. Create the following test user accounts via Admin > Accounts:    - `qa_tester@silq.test` -- assign role `admin` (all Document Control and Admin Docs permissions). This is the primary test account. - `readonly_tester@silq.test` -- assign a role with only `admin.view` and `docs.view` permissions, and NO `docs.create`, `docs.edit`, `docs.release`, or `docs.obsolete` permissions. This account is used to verify Access Control (Test Case 8).    - Record both account passwords securely for the duration of testing. 4. Confirm there are no existing documents in Document Control with the test document numbers used below (`SRS-TEST-001`, `SRS-TEST-002`). If they exist from a prior test run, use distinct numbers (e.g., `SRS-TEST-001b`) and record the substitution in the Observed Results column. 5. On the tester's laptop, prepare the following test files (any suitable content):    - `test_doc.pdf` -- a small PDF file (< 1 MB).    - `test_doc.docx` -- a small Microsoft Word file.    - `test_doc.xlsx` -- a small Microsoft Excel file.    - `test_image.png` -- a small PNG image.    - `test_data.csv` -- a small CSV file.    - `large_file.bin` -- a binary file greater than 50 MB but less than 100 MB (used for size-limit verification). 6. Have available a command-line terminal with either `curl` or a browser with developer tools (Network tab) to inspect HTTP headers and to post CSRF-protected requests. 7. Have available a utility capable of computing a SHA-256 hash of a file (e.g., `certutil -hashfile <file> SHA256` on Windows, `shasum -a 256 <file>` on macOS/Linux, or PowerShell `Get-FileHash -Algorithm SHA256`). No special server-side configuration is required beyond the standard production deployment. Silq eQMS does not have document schemas, cabinets, drawers, metadata fields, or workflow approval groups analogous to FileHold. The Silq eQMS equivalents are the 11 named Admin Docs Libraries (fixed; defined in SRS-3.1) and the Document Control module's Draft/Released/Obsolete lifecycle. | Prior to testing, the system administrator (or equivalent authorized user) shall perform the following:  1. Confirm Silq eQMS is deployed to the production environment and that the URL `https://Silq eQMS.com` (DigitalOcean App Platform deployment URL) is reachable over HTTPS in the tester's browser. 2. Record the Silq eQMS version being tested (commit SHA or release tag) on the cover page. 3. Create the following test user accounts via Admin > Accounts:    - `qa_tester@silq.test` -- assign role `admin` (all Document Control and Admin Docs permissions). This is the primary test account. - `readonly_tester@silq.test` -- assign a role with only `admin.view` and `docs.view` permissions, and NO `docs.create`, `docs.edit`, `docs.release`, or `docs.obsolete` permissions. This account is used to verify Access Control (Test Case 8).    - Record both account passwords securely for the duration of testing. 4. Confirm there are no existing documents in Document Control with the test document numbers used below (`SRS-TEST-001`, `SRS-TEST-002`). If they exist from a prior test run, use distinct numbers (e.g., `SRS-TEST-001b`) and record the substitution in the Observed Results column. 5. On the tester's laptop, prepare the following test files (any suitable content):    - `test_doc.pdf` -- a small PDF file (< 1 MB).    - `test_doc.docx` -- a small Microsoft Word file.    - `test_doc.xlsx` -- a small Microsoft Excel file.    - `test_image.png` -- a small PNG image.    - `test_data.csv` -- a small CSV file.    - `large_file.bin` -- a binary file greater than 50 MB but less than 100 MB (used for size-limit verification). 6. Have available a command-line terminal with either `curl` or a browser with developer tools (Network tab) to inspect HTTP headers and to post CSRF-protected requests. 7. Have available a utility capable of computing a SHA-256 hash of a file (e.g., `certutil -hashfile <file> SHA256` on Windows, `shasum -a 256 <file>` on macOS/Linux, or PowerShell `Get-FileHash -Algorithm SHA256`). No special server-side configuration is required beyond the standard production deployment. Silq eQMS does not have document schemas, cabinets, drawers, metadata fields, or workflow approval groups analogous to FileHold. The Silq eQMS equivalents are the 11 named Admin Docs Libraries (fixed; defined in SRS-3.1) and the Document Control module's Draft/Released/Obsolete lifecycle. |


Test Procedure

[Table]
|  | Test Case 1 – Document Creation and Lifecycle |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 1-1 | Open a modern browser and navigate to `https://Silq eQMS.com/auth/login`. Enter the email and password for the `qa_tester` account from Test Setup and click the `Sign in` button. | Silq eQMS landing page (Admin index) appears in the browser. The top navigation displays the logged-in user's email. |  | Pass      Fail |  |
| 1-2 | Navigate to `https://Silq eQMS.com/admin/modules/document-control/`. | The "Document Control" page appears with heading "Document Control," a list of existing documents (or "No documents yet."), and buttons "Back to Admin" and "New document" in the top right. |  | Pass      Fail |  |
| 1-3 | Click the `New document` button. On the "New Document" page, leave the `Document number`, `Title`, and `Document type` fields blank and click `Create`. | The form is rejected. A danger flash message appears: "doc_number, title, and doc_type are required." The page redirects back to the New Document form. Successful completion verifies requirement SRS-1.1 (all three fields required). |  | Pass      Fail |  |
| 1-4 | On the "New Document" page, enter `Document number` = `SRS-TEST-001`, `Title` = `Silq eQMS Verification Test Document`, `Document type` = `SOP`, then click `Create`. | A success flash message appears: "Document created (Draft)." The browser redirects to the document detail page for `SRS-TEST-001`. The header shows "SRS-TEST-001 -- Silq eQMS Verification Test Document" with "Type: SOP · Status: Draft · Current: Rev A." |  | Pass      Fail |  |
| 1-5 | Navigate back to `https://Silq eQMS.com/admin/modules/document-control/`. Click `New document` and attempt to create a second document with `Document number` = `SRS-TEST-001` (same as Step 1-4), `Title` = `Duplicate Test`, `Document type` = `SOP`. Click `Create`. | A danger flash message appears: "Document number already exists." The document is NOT created. |  | Pass      Fail |  |
| 1-6 | Navigate to the detail page for `SRS-TEST-001`. Locate the "Upload file" panel under Rev A. Click `Choose File`, select `test_doc.pdf` from Test Setup, and click the `Upload` button. | A success flash message appears: "File uploaded." The Files panel for Rev A now lists `test_doc.pdf` with its size in bytes and a SHA-256 prefix. |  | Pass      Fail |  |
| 1-7 | On the same detail page, in the "Upload file" panel for Rev A, attempt to upload a second file `test_image.png`. | The upload is rejected. A danger flash message appears: "This draft revision already has a file. Create a new revision instead." |  | Pass      Fail |  |
| 1-8 | On the detail page for `SRS-TEST-001` (status still Draft), locate the "Release revision" panel under Rev A. Leave the `Reason (required)` field blank. Click `Release`. | The release is rejected. A danger flash message appears: "Release requires a reason." The document status remains `Draft`. |  | Pass      Fail |  |
| 1-9 | In the "Release revision" panel, enter `Reason` = `Initial release for verification testing`, `Change summary` = `First release`, `Effective date` = today's date. Click `Release`. | A success flash message appears: "Revision released." The page reloads; the document header now shows "Status: Released." Rev A now shows a non-empty `Released` timestamp and the entered `Change summary` and `Effective date`. The "Upload file" and "Release revision" panels are no longer displayed for Rev A. |  | Pass      Fail |  |
| 1-10 | On the detail page for `SRS-TEST-001` (now Released), locate the "Create next revision" card at the bottom of the page. Click `New revision`. | A success flash message appears: "Created draft revision B. Upload a new file to continue." The document header now shows "Status: Draft · Current: Rev B." A new Rev B section appears above Rev A. |  | Pass      Fail |  |
| 1-11 | Upload `test_doc.pdf` to Rev B using the same process as Step 1-6. Then, without releasing Rev B, attempt to trigger the "Create next revision" action on the detail page. | The "Create next revision" and "Obsolete document" cards ARE still visible (because the document is not Obsolete), but clicking `New revision` in the "Create next revision" card results in a danger flash message: "You can only create a next revision from a Released document." |  | Pass      Fail |  |
| 1-12 | Create a second test document: navigate to New Document, create `Document number` = `SRS-TEST-002`, `Title` = `Obsolete Transition Test`, `Document type` = `SOP`. Do NOT upload a file. In the Rev A "Release revision" panel, enter a reason and click `Release`. | The release is rejected. A danger flash message appears: "Release requires an uploaded file." The status remains `Draft`. |  | Pass      Fail |  |
| 1-13 | On the detail page for `SRS-TEST-001`, release Rev B by uploading a file if needed and completing the Release form (`Reason` = `Second release`). Record the transition. Then open `SRS-TEST-002`, upload a file, release Rev A, then create a next revision. Verify the new revision letter assigned to `SRS-TEST-002`. | Rev B of `SRS-TEST-001` transitions `Draft → Released`. Creating the next revision of `SRS-TEST-001` assigns `Rev C`. For `SRS-TEST-002`, creating the next revision after release of Rev A assigns `Rev B`. |  | Pass      Fail |  |

Automated-test supplementary evidence: `tests/test_document_control.py::test_document_control_vertical_slice_creates_releases_downloads_and_audits` covers the create → upload → release → download path (SRS-1.1, SRS-1.2, SRS-1.3, SRS-1.10) and is attached to SW.SLQ011 as supplementary verification evidence.


[Table]
|  | Test Case 2 – Document Obsoleting |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 2-1 | Ensure `SRS-TEST-001` is in `Released` status (from Test Case 1). Navigate to its detail page. Locate the "Obsolete document" card in the bottom action grid. Leave the `Reason (required)` field blank and click `Mark obsolete`. | The action is rejected. A danger flash message appears: "Obsoleting requires a reason." The document status remains `Released`. |  | Pass      Fail |  |
| 2-2 | In the "Obsolete document" card, enter `Reason` = `Superseded by SRS-TEST-002 for verification purposes`. Click `Mark obsolete`. | A success flash message appears: "Document marked Obsolete." The detail page header now shows "Status: Obsolete." An obsolete warning banner appears below the header (see Test Case 5 for banner verification). |  | Pass      Fail |  |
| 2-3 | Navigate to `https://Silq eQMS.com/admin/audit`. In the "Action contains" filter, enter `doc.obsolete`. Click `Apply filters`. | The audit trail lists an event with Action = `doc.obsolete`, Actor = the tester's email, Entity = `Document #<id>` (where id is the SRS-TEST-001 database id), and Reason = `Superseded by SRS-TEST-002 for verification purposes`. |  | Pass      Fail |  |


[Table]
|  | Test Case 3 – Document Download and View |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 3-1 | Create a new Draft document `SRS-TEST-003` (Title = `Download/View Test`, Type = `SOP`) and upload `test_doc.pdf` to Rev A. Do NOT release. On the detail page, click the `Download` button next to the file. | The browser downloads a file named `test_doc.pdf`. The downloaded file content matches the uploaded file. |  | Pass      Fail |  |
| 3-2 | On the same detail page, click the `View` button next to `test_doc.pdf`. | The browser opens the PDF inline in a new tab or the current tab (native browser rendering for PDF). The file displays its content. |  | Pass      Fail |  |
| 3-3 | Release Rev A of `SRS-TEST-003` with a reason (`Initial release`). Click `Download` for the released file. Click `View` for the released file. | Download serves the file as an attachment with the original filename. View opens the PDF inline. |  | Pass      Fail |  |
| 3-4 | Create a Draft document `SRS-TEST-004` (Title = `Office Formats Test`, Type = `FORM`) and upload `test_doc.docx` to Rev A. Click `View` next to the .docx file | The browser displays an HTML-rendered version of the Word document (server-rendered, not a raw download). A visible "Download original" link is available on the page. |  | Pass      Fail |  |
| 3-5 | On `SRS-TEST-004`, create a second revision or a separate Draft document and upload `test_doc.xlsx`. Click `View`. | The browser displays an HTML-rendered view of the Excel workbook. |  | Pass      Fail |  |
| 3-6 | Upload a `.csv` file to a new Draft document and click `View`. | The browser displays an HTML-rendered tabular view of the CSV. |  | Pass      Fail |  |
| 3-7 | Upload `test_image.png` to a new Draft document and click `View`. | The browser displays the image inline. |  | Pass      Fail |  |
| 3-8 | Return to the detail page for `SRS-TEST-001` (now Obsolete from Test Case 2). Click `Download` for the Rev A file, then click `View` for the same file. | Both actions succeed. Download returns the file as an attachment. View opens the file inline / server-rendered. |  | Pass      Fail |  |
| 3-9 | For each file downloaded in Steps 3-1 through 3-8, compare the downloaded filename to the originally uploaded filename. | In all cases, the downloaded filename matches the original uploaded filename (the file is served as an attachment with the original filename). |  | Pass      Fail |  |


[Table]
|  | Test Case 4 – File Integrity (SHA-256 and File Size) |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 4-1 | Before uploading, compute the SHA-256 hash and file size (bytes) of `test_doc.pdf` on the tester's workstation. Record both values. For example, on Windows: `certutil -hashfile test_doc.pdf SHA256`. On macOS/Linux: `shasum -a 256 test_doc.pdf` and `wc -c test_doc.pdf`. | Tester records the computed SHA-256 hash (64 hex characters) and file size (bytes) in the Observed Results column. |  | Pass      Fail |  |
| 4-2 | Create a new Draft document `SRS-TEST-005` (Title = `File Integrity Test`, Type = `SOP`) and upload the same `test_doc.pdf` to Rev A. On the detail page, read the displayed file metadata below the filename, which shows `<size_bytes> bytes · sha256 <first-12-hex-chars>…`. Record the displayed file size and SHA-256 prefix. | The displayed file size (bytes) equals the independently computed file size from Step 4-1. The displayed SHA-256 prefix matches the first 12 characters of the independently computed SHA-256 from Step 4-1. |  | Pass      Fail |  |
| 4-3 | Download the file using the `Download` button. Compute the SHA-256 hash of the downloaded file using the same method as Step 4-1. Compare to the hash computed in Step 4-1. | The SHA-256 of the downloaded file equals the SHA-256 computed in Step 4-1 (and therefore matches the stored hash displayed in Step 4-2). |  | Pass      Fail |  |
| 4-4 | Optional — If the tester has read access to the production database, connect to the document_files table and query the row for the file uploaded in Step 4-2. Inspect the columns sha256, size_bytes, filename, and content_type. Alternative without DB access: open /admin/audit, click View next to the doc.upload event for the same file, and read metadata_json (it contains sha256 and size_bytes | sha256 contains the full 64-character SHA-256 digest (matching Step 4-1). size_bytes matches Step 4-1. filename = test_doc.pdf. content_type = application/pdf. Successful completion further verifies SRS-1.9. |  | Pass      Fail |  |


[Table]
|  | Test Case 5 – Obsolete Document Safeguards |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 5-1 | Navigate to `https://Silq eQMS.com/admin/modules/document-control/`. Locate the entry for `SRS-TEST-001` (marked Obsolete in Test Case 2). | The `SRS-TEST-001` card displays a red pill-shaped badge with the text `OBSOLETE` (background color red (#dc3545), white text, bold). Non-obsolete documents display only the plain text status (e.g., "Draft," "Released") without a red badge. |  | Pass      Fail |  |
| 5-2 | Click the `SRS-TEST-001` card to open the detail page. | Below the document header, a prominent pink/red warning banner appears with the bolded text `This document is obsolete.` followed by `It is retained for historical reference only. Do not use this document for current operations.` Below that, a `Reason:` line displays the reason entered in Test Case 2 Step 2-2 (`Superseded by SRS-TEST-002 for verification purposes`). |  | Pass      Fail |  |
| 5-3 | On the same detail page, scroll to the bottom of the page. Inspect the action cards area. | Neither the "Create next revision" card nor the "Obsolete document" card is displayed. (The per-revision "Upload file" and "Release revision" panels are also not displayed because the document is not Draft.) |  | Pass      Fail |  |
| 5-4 | Click `Download` on any released file of `SRS-TEST-001`. Then navigate to `https://Silq eQMS.com/admin/audit` and filter Action contains = `doc.download_obsolete`. | The audit trail records an event with Action = `doc.download_obsolete` (NOT `doc.download`), Actor = tester's email, Entity = `DocumentFile #<file_id>`. |  | Pass      Fail |  |
| 5-5 | Return to the detail page for `SRS-TEST-001`. Click `View` on any released file. Return to the audit trail and filter Action contains = `doc.view_obsolete`. | The audit trail records an event with Action = `doc.view_obsolete` (NOT `doc.view`), Actor = tester's email, Entity = `DocumentFile #<file_id>`. |  | Pass      Fail |  |

Automated-test supplementary evidence: `tests/test_edms_improvements.py::test_obsolete_document_download_creates_distinct_audit_event` (SRS-2.3) and `tests/test_edms_improvements.py::test_obsolete_document_view_creates_distinct_audit_event` (SRS-2.4) provide additional verification.


[Table]
|  | Test Case 6 – Admin Docs Library Operations |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 6-1 | Navigate to `https://Silq eQMS.com/admin/` (Admin index). From the Admin navigation / side menu, visit each of the eleven Admin Docs libraries at the following URLs and record the H1 page title shown on each:  1. `/admin/qms-documents`  2. `/admin/employee-training`  3. `/admin/management-reviews`  4. `/admin/ncrs`  5. `/admin/capas`  6. `/admin/post-market-surveillance`  7. `/admin/regulatory-standards`  8. `/admin/work-orders`  9. `/admin/risk-management`  10. `/admin/dhfs`  11. `/admin/forms-templates-travelers` | Each URL returns HTTP 200 and renders a library page. The displayed page titles exactly match the SRS-3.1 names:  1. `Quality Management Documents`  2. `Employee Training`  3. `Management Reviews, Audits & Approvals`  4. `NCRs`  5. `CAPAs`  6. `Post Market Surveillance`  7. `Regulatory Standards & Approvals`  8. `Work Orders`  9. `Risk Management`  10. `Design History Files (DHFs)`  11. `Forms, Templates & Travelers` |  | Pass      Fail |  |
| 6-2 | Navigate to the `Quality Management Documents` library (`/admin/qms-documents`). In the "Create Folder" form at the top of the page, enter `Folder name` = `SRS Test Folder` and click `Create Folder`. | A success flash message appears: "Folder created." The `SRS Test Folder` row appears under the Folders heading. |  | Pass      Fail |  |
| 6-3 | Click the `SRS Test Folder` link to navigate into it. In the Create Folder form, enter `Folder name` = `Subfolder 1` and click `Create Folder`. | A success flash message appears: "Folder created." `Subfolder 1` appears under Folders. The breadcrumb trail at the top of the page now shows `Root / SRS Test Folder`. |  | Pass      Fail |  |
| 6-4 | Click the `Subfolder 1` link. Verify the breadcrumb shows `Root / SRS Test Folder / Subfolder 1` and that each crumb is a clickable link. Click `SRS Test Folder` in the breadcrumb. | The page navigates back to `SRS Test Folder` (with only `Root / SRS Test Folder` breadcrumb). |  | Pass      Fail |  |
| 6-5 | In `SRS Test Folder`, scroll to the "Upload Document" section at the bottom. Click `Choose File`, select `test_doc.pdf`, optionally enter a Description, then click `Upload`. | A success flash message appears: "Successfully uploaded 1 document(s)." The file `test_doc.pdf` appears in the Documents table with its upload date. |  | Pass      Fail |  |
| 6-6 | In the "Upload Document" section, click `Choose File` and select multiple files at once (hold Ctrl/Cmd): `test_doc.docx`, `test_image.png`, and `test_data.csv`. Click `Upload`. | A success flash message appears: "Successfully uploaded 3 document(s)." All three files appear in the Documents table. |  | Pass      Fail |  |
| 6-7 | Attempt to upload `large_file.bin` (>50 MB). | The upload is rejected with a warning flash message that includes the text "too large (max 50MB)" for the file. The file does NOT appear in the Documents table. |  | Pass      Fail |  |
| 6-8 | In the Documents table, click `Download` next to `test_doc.pdf`. | The browser downloads a file named `test_doc.pdf` matching the uploaded content. |  | Pass      Fail |  |
| 6-9 | Click `View` next to `test_doc.pdf`. | The browser opens the PDF inline. |  | Pass      Fail |  |
| 6-10 | Click `View` next to `test_doc.docx`. | The browser displays an HTML-rendered view of the Word document. |  | Pass      Fail |  |
| 6-11 | Click `View` next to `test_data.csv`. | The browser displays an HTML-rendered tabular view of the CSV. |  | Pass      Fail |  |
| 6-12 | Click `View` next to `test_image.png`. | The browser displays the image inline. |  | Pass      Fail |  |
| 6-13 | Click the `Move` button next to `test_doc.pdf`. In the Move modal, select `Library` = `NCRs` and `Folder` = `— Root —`. Click `Move`. | A success flash message appears: "Document moved to NCRs." The browser redirects to the NCRs library root. The file `test_doc.pdf` now appears in the Documents table at the NCRs root. The file no longer appears in `Quality Management Documents / SRS Test Folder`. |  | Pass      Fail |  |
| 6-14 | In the NCRs library, click `Move` on `test_doc.pdf` again. In the Move modal, select `Library` = `Quality Management Documents` and `Folder` = `Quality Management Documents / SRS Test Folder`. Click `Move`. | The file is moved back to the original folder. |  | Pass      Fail |  |


[Table]
|  | Test Case 7 – Authentication and Session Security |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 7-1 | Open a private/incognito browser window. Navigate to `https://Silq eQMS.com/admin/modules/document-control/`. | The browser is redirected to `https://Silq eQMS.com/auth/login?next=...`. The login form is displayed. |  | Pass      Fail |  |
| 7-2 | At the login form, enter a valid email (`qa_tester@silq.test`) and a valid password. Click `Sign in`. | The browser is redirected to the Admin index (or the originally requested page via the `next` parameter). The top navigation displays the logged-in user's email. |  | Pass      Fail |  |
| 7-3 | Click the `Logout` link (or navigate to `https://Silq eQMS.com/auth/logout`). | The browser is redirected to the public index. Attempting to access `https://Silq eQMS.com/admin/` now redirects to the login form. |  | Pass      Fail |  |
| 7-4 | Navigate to the login form. Enter a valid email (`qa_tester@silq.test`) with an invalid password. Click `Sign in`. | The page reloads showing a danger flash message: "Invalid credentials." The user is NOT logged in. |  | Pass      Fail |  |
| 7-5 | Using a single browser/IP, repeat Step 7-4 four more times (for a total of 5 failed attempts) within 5 minutes. Then attempt a 6th login (either valid or invalid credentials). | On the 6th attempt, a danger flash message appears: "Too many login attempts. Please wait 5 minutes." No login is allowed from this IP until the 5-minute window has elapsed. |  | Pass      Fail |  |
| 7-6 | Wait 5 minutes. Log in successfully with valid credentials. | Login succeeds |  | Pass      Fail |  |
| 7-7 | Navigate to `https://Silq eQMS.com/admin/audit`. Filter Action contains = `auth.login`, then `auth.logout`, then `auth.login_failed`. | The audit trail contains: -An `auth.login` event (successful login) with Actor = tester's email. -An `auth.logout` event with Actor = tester's email. -Multiple `auth.login_failed` events with entity_id / metadata containing the attempted email (`qa_tester@silq.test`). Each has a UTC timestamp. |  | Pass      Fail |  |
| 7-8 | (Verification inspection.) With database-read access, inspect the `users` table for the `qa_tester@silq.test` row. Inspect the `password_hash` column. | The `password_hash` column contains a Werkzeug-formatted hashed value (e.g., beginning with `pbkdf2:sha256:` or `scrypt:`), NOT the plaintext password. |  | Pass      Fail |  |
| 7-9 | (Session timeout.) Log in to Silq eQMS. Record the start time. Leave the browser idle (no mouse, no keyboard, no requests) for 60 minutes. After 60 minutes of inactivity, attempt to navigate to `https://Silq eQMS.com/admin/modules/document-control/`. | The browser is redirected to the login form. The tester must log in again. Record the idle start and end times. |  | Pass      Fail |  |
| 7-10 | (Sliding window verification.) Log in again. Over the course of 30-45 minutes, click through several pages at roughly 10-minute intervals. Record the time of the last click. After the last click, wait an additional 60 minutes and then attempt to navigate to a protected page. | During the active-usage period, the session remains valid (no redirect to login). Only after a continuous 60-minute idle period following the last activity is the tester redirected to login. |  | Pass      Fail |  |

Automated-test supplementary evidence: `tests/test_edms_improvements.py::test_session_timeout_is_60_minutes` verifies the `PERMANENT_SESSION_LIFETIME` configuration value (SRS-4.3). Note: if the current code sets the session lifetime to a value other than 60 minutes, record the observed configured value and flag as a finding.


[Table]
|  | Test Case 8 – Access Control |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 8-1 | Ensure you are logged out. Navigate directly to `https://Silq eQMS.com/admin/modules/document-control/`. | The browser is redirected to `https://Silq eQMS.com/auth/login?next=%2Fadmin%2Fmodules%2Fdocument-control%2F` (the `next` parameter preserves the original path). |  | Pass      Fail |  |
| 8-2 | Repeat Step 8-1 for `https://Silq eQMS.com/admin/audit` and `https://Silq eQMS.com/admin/ncrs`. | Both URLs redirect to the login form. |  | Pass      Fail |  |
| 8-3 | Log in as `readonly_tester@silq.test` (the account created in Test Setup with only `admin.view` and `docs.view` permissions and no docs.create / docs.edit / docs.release / docs.obsolete). Navigate to `https://Silq eQMS.com/admin/modules/document-control/` and click `New document`. Attempt to submit the New Document form with valid data. | Accessing `/admin/modules/document-control/new` (GET) or POSTing to it produces an HTTP 403 Forbidden response. The 403 error page is displayed. |  | Pass      Fail |  |
| 8-4 | Still logged in as `readonly_tester`, attempt to access `https://Silq eQMS.com/admin/accounts/` or another route protected by `admin.edit` (e.g., attempt a POST to `/admin/accounts/new`). | The response is HTTP 403 Forbidden. |  | Pass      Fail |  |
| 8-5 | Log out and log back in as `qa_tester`. Navigate to `https://Silq eQMS.com/admin/modules/document-control/` and click `New document`. | The New Document form is displayed. The tester can submit it successfully. |  | Pass      Fail |  |


[Table]
|  | Test Case 9 – Audit Trail |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 9-1 | Logged in as `qa_tester`, navigate to `https://Silq eQMS.com/admin/audit`. | The Audit Trail page renders with a filter form (Action contains, Actor email contains, Date from, Date to, Apply filters, Clear, Export CSV buttons) and a table with columns `Time`, `Actor`, `Action`, `Entity`, `Reason`. |  | N/A |  |
| 9-2 | Review the audit table for entries created during Test Cases 1-8. Enter no filters and click `Apply filters`. Scroll/search through the events. | At minimum, the following action types appear (each with a UTC timestamp, actor email, entity type/ID, and reason where applicable): `auth.login`, `auth.logout`, `auth.login_failed`, `doc.create`, `doc.upload`, `doc.release`, `doc.revise`, `doc.obsolete`, `doc.download`, `doc.view`, `doc.download_obsolete`, `doc.view_obsolete`, `admin_docs.folder.create`, `admin_docs.file.upload`. |  | Pass      Fail |  |
| 9-3 | Click any audit row (or inspect via the database) and confirm the record contains: timestamp (UTC), actor email, action, entity type, entity id, reason (where applicable), metadata_json (JSON string), client_ip, and request_id. | All listed fields are present and populated for each event (metadata_json is a valid JSON string; client_ip is an IP address; request_id is a hex string). |  | Pass      Fail |  |
| 9-4 | In the filter form, enter `Action contains` = `doc.release` and click `Apply filters`. | Only `doc.release` events appear in the table. |  | Pass      Fail |  |
| 9-5 | Clear filters. Enter `Actor email contains` = `qa_tester` and click `Apply filters`. | Only events with actor email containing `qa_tester` appear. |  | Pass      Fail |  |
| 9-6 | Clear filters. Enter `Date from` = today's date and `Date to` = today's date. Click `Apply filters`. | Only events with timestamps within today (UTC) appear. |  | Pass      Fail |  |
| 9-7 | With filters set to Actor email = `qa_tester` and today's date range, click `Export CSV`. | The browser downloads a file named `audit_export_YYYY-MM-DD.csv`. The content type is `text/csv`. |  | Pass      Fail |  |
| 9-8 | Open the downloaded CSV in a spreadsheet editor or text editor. Inspect the header row. | The header row contains exactly the following columns, in order: `id, created_at, action, actor_user_email, entity_type, entity_id, reason, metadata_json, client_ip, request_id`. |  | Pass      Fail |  |
| 9-9 | Count the data rows in the CSV. Confirm all matching events are present (no row cap). | All events matching the applied filters are present in the CSV (not limited to the 500 shown in the web UI). |  | Pass      Fail |  |
| 9-10 | Return to the Audit Trail page and filter `Action contains` = `audit.export`. | An `audit.export` event is present with Actor = tester's email and `metadata_json` containing the filters used (`action`, `actor_email`, `date_from`, `date_to`) and the `event_count`. |  | Pass      Fail |  |
| 9-11 | Inspect the Audit Trail UI (and, separately, the detail page / database if available). Look for any `Edit`, `Delete`, or `Modify` button, link, or form action on audit rows. | No edit or delete action is available anywhere in the UI for audit events. The audit view is read-only. |  | Pass      Fail |  |

Automated-test supplementary evidence: `tests/test_edms_improvements.py::test_audit_trail_csv_export` (SRS-6.4 and partial SRS-6.5) and `tests/test_edms_improvements.py::test_audit_trail_csv_export_with_date_filter` (partial SRS-6.3) provide additional verification.


[Table]
|  | Test Case 10 – Security Controls (CSRF and Security Headers) |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 10-1 | Logged in as `qa_tester`, open the browser's developer tools (F12). Open the Network tab. Navigate to the New Document form at `https://Silq eQMS.com/admin/modules/document-control/new`. Submit the form with valid data. In the Network tab, inspect the POST request. | The POST request body includes a `csrf_token` form field with a non-empty URL-safe token. The server returns HTTP 302 (redirect to the document detail page) -- i.e., the request is accepted. |  | Pass      Fail |  |
| 10-2 | Using `curl` from a terminal (or a tool such as Postman/Insomnia), send a POST request to `https://Silq eQMS.com/admin/modules/document-control/new` WITHOUT a `csrf_token` form field. Include the session cookie from the logged-in browser session (copy it from DevTools > Application > Cookies). Example command: `curl -X POST "https://Silq eQMS.com/admin/modules/document-control/new" -d "doc_number=CSRF-TEST&title=CSRF&doc_type=SOP" -b "session=<cookie>"`. | The server returns HTTP 400. The response body contains the text `CSRF token missing or invalid.` The document is NOT created. |  | Pass      Fail |  |
| 10-3 | Repeat Step 10-2 but POST to a different state-changing endpoint, for example `/admin/modules/document-control/<doc_id>/obsolete` with form field `reason=Test`. Omit `csrf_token`. | HTTP 400 with the CSRF error message. Further confirms **SRS-7.1** applies to all state-changing endpoints (POST/PUT/PATCH/DELETE). |  | Pass      Fail |  |
| 10-4 | Open a fresh incognito browser (not logged in). Using `curl` or a browser form, POST to `https://Silq eQMS.com/auth/login` with `email` and `password` form fields but NO `csrf_token`. | The POST is processed (not a 400 CSRF error). The response is either a successful login redirect (for valid credentials) or a re-display of the login form with "Invalid credentials" (for invalid credentials). |  | Pass      Fail |  |
| 10-5 | Open the browser developer tools > Network tab. Navigate to any Silq eQMS page (e.g., `https://Silq eQMS.com/admin/`). Select the main document request and inspect the response headers. | All four of the following headers are present on the response: `Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; frame-src 'none'; object-src 'none'` `X-Content-Type-Options: nosniff`  `X-Frame-Options: DENY` `Referrer-Policy: strict-origin-when-cross-origin` |  | Pass      Fail |  |
| 10-6 | Repeat Step 10-5 on the login page (`/auth/login`) and on a static page / API endpoint. | Each inspected response carries the same four security headers |  | Pass      Fail |  |


[Table]
|  | Test Case 11 – Data Integrity (No-Delete Verification) |  |  |  |  |
| --- | --- | --- | --- | --- | --- |
| Step # | Procedural Steps | Expected results | Observed results | Result (Circle one) | Performed by Initials / Date |
| 11-1 | Navigate to `https://Silq eQMS.com/admin/modules/document-control/`. Inspect the document list page for any `Delete`, `Remove`, or `Trash` button, link, or checkbox-batch action. | No delete control is visible anywhere on the document list page. |  | Pass      Fail |  |
| 11-2 | Open the detail page for any document (Draft, Released, and Obsolete). Inspect every panel, card, button, and form. | No delete control exists on the detail page for documents, revisions, or files in any document status. The "Create next revision" and "Obsolete document" cards do NOT offer deletion. |  | Pass      Fail |  |
| 11-3 | Navigate to each of the 11 Admin Docs libraries (Step 6-1 list). On each library root page and within folders, inspect the Documents table, folder rows, and action buttons. | No delete button, link, or batch action exists for files or folders in Admin Docs. Only `View`, `Download`, and `Move` actions are available for files. |  | Pass      Fail |  |
| 11-4 | Navigate to `https://Silq eQMS.com/admin/modules/document-control/`. Confirm the Obsolete documents from previous test cases (e.g., `SRS-TEST-001`) are still visible in the list. | All Obsolete documents continue to appear in the document list with the red `OBSOLETE` badge. No Obsolete document is hidden, filtered out, or removed. |  | Pass      Fail |  |
| 11-5 | Open the detail page for an Obsolete document. Confirm the file(s) are still present and that `Download` and `View` buttons function (subject to Test Case 5 audit verification). | he Obsolete document's revisions and files remain fully accessible. |  | Pass      Fail |  |


SRS Coverage Cross-Reference:
SRS-1.1   Test Case 1, Steps 1-3, 1-4
  SRS-1.2   Test Case 1, Steps 1-6, 1-7
  SRS-1.3   Test Case 1, Steps 1-8, 1-9, 1-12
  SRS-1.4   Test Case 1, Steps 1-10, 1-11
  SRS-1.5   Test Case 2, Steps 2-1, 2-2, 2-3
  SRS-1.6   Test Case 3, Steps 3-1, 3-3, 3-8, 3-9
  SRS-1.7   Test Case 3, Steps 3-2, 3-4, 3-5, 3-6, 3-7
  SRS-1.8   Test Case 1, Steps 1-9, 1-10, 1-13
  SRS-1.9   Test Case 4, Steps 4-1, 4-2, 4-3, 4-4
  SRS-1.10   Test Case 1, Step 1-5
  SRS-1.11   Test Case 1, Steps 1-10, 1-13
  SRS-2.1   Test Case 5, Step 5-1
  SRS-2.2   Test Case 5, Step 5-2
  SRS-2.3   Test Case 5, Step 5-4
  SRS-2.4   Test Case 5, Step 5-5
  SRS-2.5   Test Case 5, Step 5-3
  SRS-3.1   Test Case 6, Step 6-1
  SRS-3.2   Test Case 6, Steps 6-2, 6-3
  SRS-3.3   Test Case 6, Step 6-2
  SRS-3.4   Test Case 6, Steps 6-5, 6-6, 6-7
  SRS-3.5   Test Case 6, Step 6-8
  SRS-3.6   Test Case 6, Steps 6-9, 6-10, 6-11, 6-12
  SRS-3.7   Test Case 6, Steps 6-13, 6-14
  SRS-3.8   Test Case 6, Steps 6-3, 6-4
  SRS-4.1   Test Case 7, Steps 7-1, 7-2
  SRS-4.2   Test Case 7, Step 7-8
  SRS-4.3   Test Case 7, Steps 7-9, 7-10
  SRS-4.4   Test Case 7, Steps 7-5, 7-6
  SRS-4.5   Test Case 7, Step 7-7
  SRS-5.1   Test Case 8, Step 8-6
  SRS-5.2   Test Case 8, Steps 8-1, 8-2, 8-3, 8-4
  SRS-5.3   Test Case 8, Step 8-5
  SRS-6.1   Test Case 9, Step 9-2
  SRS-6.2   Test Case 9, Step 9-3
  SRS-6.3   Test Case 9, Steps 9-4, 9-5, 9-6
  SRS-6.4   Test Case 9, Steps 9-7, 9-8, 9-9
  SRS-6.5   Test Case 9, Step 9-10
  SRS-6.6   Test Case 9, Step 9-11
  SRS-7.1   Test Case 10, Steps 10-1, 10-2, 10-3, 10-4
  SRS-7.2   Test Case 10, Steps 10-5, 10-6
  SRS-8.1   Test Case 11, Steps 11-1, 11-2
  SRS-8.2   Test Case 11, Step 11-3
SRS-8.3   Test Case 11, Steps 11-4, 11-5
End of Test Procedure
