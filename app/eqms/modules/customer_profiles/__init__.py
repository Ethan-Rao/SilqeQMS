"""
Customer Profiles module (admin-only).

P0 scope:
- Customers CRUD (list + create + detail/update)
- Optional lightweight customer notes
- Primary rep assignment via customers.primary_rep_id (no many-to-many yet)
"""

